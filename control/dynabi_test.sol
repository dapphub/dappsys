import 'dappsys/sig_helper.sol';
import 'dappsys/control/protected.sol';

contract DSDynABI is DSProtected
                   , DSSigHelperMixin
{
    function _ds_set_handler( bytes4 sig, address handler )
             auth() 
             returns (bool)
    {
        
    }
    function() {
        
    }
}
